- name: ROSA Fruits App Demo Setup
  hosts: localhost
  connection: local

  vars:
    rollback: true

  vars_prompt:
    - name: aws_access_key
      prompt: Enter AWS Access Key

    - name: aws_secret_key
      prompt: Enter AWS Secret Key

  roles:
    - oidc